# NPS 重构项目目录说明

本目录用于维护 NPS 重构版。后续换对话或换电脑时，先看本文件和根目录开发文档。

## 根目录文件

```text
AGENTS.md                Codex/开发协作入口说明
DEVELOPMENT_DESIGN.md    设计方案，记录 ID 池、portForward、SOCKS 规则
DEVELOPMENT_MANUAL.md    开发手册，记录实现状态、测试命令、部署注意事项
代码说明.md              当前源码改动说明和核心文件地图
API说明文档.md           后期管理平台调用 NPS API 的说明
目录说明.md              当前目录结构说明
```

## 源码与工作区

```text
source/nps               上游干净源码，只用于对照
workspace/nps-dev        开发源码，所有代码修改都在这里
workspace/build          构建产物
conf                     当前参考配置和备份
tools/go                 本地 Go 工具链
```

## 构建产物

```text
workspace/build/windows_amd64
  nps.exe
  npc.exe
  web/

workspace/build/linux_amd64
  nps
  npc
  web/
```

部署服务端时至少需要：

```text
nps 或 nps.exe
web/
conf/
```

`web/views` 和 `web/static` 不会嵌入二进制，页面模板、语言包、CSS、JS、字体图标都依赖完整 `web` 目录。

## 当前核心规则

```text
1. Client、Host、每种隧道模式都有独立 ID 池。
2. 新增对象取当前池内最小可用正整数。
3. TCP 和 UDP 合并为 portForward。
4. 现有 NPC 不需要修改。
5. SOCKS 自动绑定客户端，端口为 10000 + Client.Id。
6. SOCKS 页面只能查看和开关，不能手动新增、编辑、删除。
```

## 开发顺序

```text
1. 阅读 AGENTS.md。
2. 阅读 DEVELOPMENT_DESIGN.md、DEVELOPMENT_MANUAL.md、代码说明.md。
3. 在 workspace/nps-dev 修改源码。
4. 运行测试。
5. 构建到 workspace/build/<platform>。
6. 同步完整 web 目录到构建产物。
```

PowerShell 找不到 `go` 时：

```powershell
$repo = (Resolve-Path '.').Path
$env:Path = (Join-Path $repo 'tools\go\bin') + ';' + $env:Path
```
