# NPS 重构版代码说明

本文说明当前 `workspace/nps-dev` 中已经完成的核心改动，方便后续换对话、换电脑或继续开发管理平台时快速接手。

## 总体目标

重构版 NPS 作为后期设备管理平台的后端。核心目标是 ID 稳定、可预测、可复用：

```text
已有 ID: 1, 2, 3, 4
删除 ID: 2
下次新增: 2
```

不再使用旧的 `max(id)+1` 漂移逻辑。

## 数据模型

独立 ID 池：

```text
Client.Id
Host.Id
portForward.Id
socks5.Id
httpProxy.Id
secret.Id
p2p.Id
file.Id
```

任务内部唯一键：

```text
mode:id
```

示例：

```text
portForward:1
socks5:1
httpProxy:1
```

这样不同功能可以同时拥有自己的 ID 1，不会互相冲突。

## 端口转发

TCP 隧道和 UDP 隧道已经合并为一个用户侧功能：`portForward`。

一个端口转发规则会同时监听：

```text
TCP server_port
UDP server_port
```

现有 NPC 不需要改。服务端收到旧配置里的 `mode=tcp` 或 `mode=udp` 时，会保存为 `portForward`；实际转发时仍然按原 NPC 协议发送：

```text
TCP -> CONN_TCP
UDP -> CONN_UDP
```

核心文件：

```text
lib/file/db.go                 模式归一和任务 ID 池
server/proxy/port_forward.go   同端口 TCP+UDP 服务封装
server/proxy/tcp.go            TCP 转发仍发送 CONN_TCP
server/proxy/udp.go            UDP 转发仍发送 CONN_UDP
bridge/bridge.go               NPC 配置入口归一为 portForward
server/server.go               运行态按 mode:id 管理
```

## SOCKS 管理规则

SOCKS 由客户端自动托管，不再手动新增、编辑、删除。

绑定关系：

```text
socks5.Id        = Client.Id
socks5.Client.Id = Client.Id
socks5.Port      = 10000 + Client.Id
socks5.Remark    = Client.Remark
```

行为：

```text
新增客户端 -> 自动创建关闭状态的 SOCKS
修改客户端备注 -> 同步 SOCKS 备注
删除客户端 -> 删除对应 SOCKS
SOCKS 页面 -> 只能查看和开关
无流量 30 分钟 -> 自动关闭 SOCKS 并持久化 Status=false
```

核心文件：

```text
lib/file/db.go
server/server.go
server/proxy/socks5.go
web/controllers/client.go
web/controllers/index.go
web/views/index/list.html
```

## ID 分配入口

只允许在这些保存入口分配 ID：

```text
DbUtils.NewClient
DbUtils.NewHost
DbUtils.NewTask
```

这些入口会扫描当前池内已有 ID，分配最小可用正整数。调用方传入的正数 ID 会被忽略。

隐藏客户端：

```text
NoStore && NoDisplay
```

使用负数 ID，不占用可见客户端 ID 池。

读取既有 `tasks.json` 时，如果同一个任务池里已经出现重复的 `mode:id`，
后读取的记录不会被静默丢弃，而是迁移到该模式当前最小可用正整数 ID 并立即写回。

## Basic 认证规则

Basic 认证用户名和密码只能由服务端管理侧配置：

```text
Web/API 客户端新增/编辑里的 u/p -> 有效
NPC 配置里的 basic_username/basic_password -> 可以继续存在，但 NPS 不采信
NPC 上报的 basic_username/basic_password -> 服务端入库前清空
```

核心文件：

```text
lib/conn/conn.go
lib/conn/conn_refactor_test.go
```

## Web 改动

公共布局已删除：

```text
顶部“欢迎使用 NPS”
左侧“使用说明”
底部版权
右下“更多说明 进入”
```

登录页已保留居中登录框，并删除原说明区、标题区、页脚区。

客户端列表的唯一验证密钥默认显示为 `********`，点击眼睛按钮后才在当前行显示真实值。展开详情里的 NPC 命令同样默认隐藏 `vkey`。该改动只影响 Web 显示，不改变 API 返回字段。

注意：NPS 不会把模板和静态文件打包进二进制。部署时必须复制完整 `web` 目录。

## API 使用方向

后期管理平台调用任务接口时必须传 `type`：

```text
type=portForward
type=socks5
type=httpProxy
type=secret
type=p2p
type=file
```

端口转发只使用：

```text
type=portForward
```

不要再拆成 TCP/UDP 两套 API 或两套 ID 池。

## 构建产物

```text
workspace/build/windows_amd64
workspace/build/linux_amd64
```

部署 Linux 服务端时通常使用：

```text
workspace/build/linux_amd64/nps
workspace/build/linux_amd64/web/
```

现有 NPC 客户端不需要替换。

## 二进制体积与代码审查

本次审查确认：二进制明显变大不是因为新增依赖或把 `web` 打进了程序。

结论：

```text
go.mod / go.sum 未新增依赖
nps 依赖图与上游一致
web/views 和 web/static 仍然是外部目录，不会嵌入二进制
默认 go build 会保留调试符号，release 构建必须使用 -trimpath -ldflags='-s -w'
```

当前 release 构建产物：

```text
workspace/build/windows_amd64/nps.exe  约 13.98 MB
workspace/build/linux_amd64/nps        约 13.55 MB
```

按 MiB 计算：

```text
workspace/build/windows_amd64/nps.exe  13.33 MiB
workspace/build/linux_amd64/nps        12.93 MiB
```

本次重构只交付 NPS 服务端二进制；现有原版或第三方 NPC 不需要重新编译或替换。

本次轻量化和审查修正：

```text
server.AddTask 的 flowSession 只启动一次，避免每个任务重复开启快照协程
异步启动失败日志不再假设所有任务都有 Client，避免 nil 指针风险
修正 go vet 指出的低风险问题：地址结构体具名字段、KCP listener unreachable return、HTTP proxy WaitGroup.Add 位置
server.AddTask/NewMode 入口统一归一 tcp/udp -> portForward，避免运行表出现旧 key
tasks.json 加载时同池重复任务 ID 会迁移到最小可用 ID，不再静默丢任务
```

已通过：

```text
go test -v ./lib/file ./lib/conn ./server ./server/proxy ./server/tool ./server/test ./web/controllers ./web/routers
go test ./bridge ./client ./cmd/npc ./cmd/nps ./lib/cache ./lib/common ./lib/conn ./lib/crypt ./lib/daemon ./lib/file ./lib/goroutine ./lib/install ./lib/rate ./lib/sheap ./lib/version ./server ./server/connection ./server/proxy ./server/test ./server/tool ./web/controllers ./web/routers
go vet ./bridge ./cmd/nps ./lib/cache ./lib/common ./lib/conn ./lib/crypt ./lib/daemon ./lib/file ./lib/goroutine ./lib/install ./lib/rate ./lib/sheap ./lib/version ./server ./server/connection ./server/proxy ./server/test ./server/tool ./web/controllers ./web/routers
```
